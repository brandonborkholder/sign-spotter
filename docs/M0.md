# M0: finish the real-submission capture

Status: request captured; awaiting the response and CORS response headers

The metadata probe and a legitimate `request_submit` POST have now been
captured. The sanitized request evidence is in
`docs/publicstuff-request-submit-redacted.txt`.

## Next steps

1. Sign out of the official PublicStuff page. The captured request contained an
   `api_key` even though no login prompt appeared. Treat that value as a private
   session credential; do not save it in the repository or reuse the value that
   was shared in chat.
2. In Chrome DevTools → **Network**, select the `OPTIONS` request immediately
   preceding `/api/2.0/request_submit`.
3. Record its HTTP status and these response headers:
   - `Access-Control-Allow-Origin`
   - `Access-Control-Allow-Headers`
   - `Access-Control-Allow-Methods`
4. Add those values to the **Missing CORS response evidence** section of
   `docs/publicstuff-request-submit-redacted.txt`.
5. Select the successful `POST /api/2.0/request_submit` request and open its
   **Response** tab.
6. Save that JSON as:

   ```text
   docs/publicstuff-request-submit-response.json
   ```

   The real request ID may remain. Remove any contact information, image data,
   or credential if the response unexpectedly contains one.
7. Tell Codex to finish M0 after both files are present.

Do not save the original cURL command or a raw HAR in the repository. The
original capture contains contact information, photograph data, and a live or
formerly live `api_key`.
