{
  "name": "sign-vigilante",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && npm run test:safety",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:safety": "node scripts/assert-no-submit-endpoint.mjs",
    "icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
